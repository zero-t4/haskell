-- Tab `    ` is equal to 8 ` ` spaces

-- ghci is a main command to run `glasgow haskell compiler interactive`

-- ghci's prompt can be changed by `:set prompt "Awesome prompt> " command

-- To load file pass its name to a ghci Hello.hs

-- load module
-- `:load Hello.hs`
-- `:l Hello.hs`

-- `:reload`
-- `:r`